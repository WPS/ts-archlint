{
  "name": "ts-archlint",
  "version": "0.0.0-SNAPSHOT",
  "description": "Simple dependency checker for typescript files",
  "main": "dist/index.js",
  "bin": {
    "archlint": "dist/index.js"
  },
  "files": [
    "/dist",
    "/doc"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "jest --config ./jest.config.js --collectCoverage --runInBand",
    "test:watch": "jest --config ./jest.config.js --watchAll",
    "archlint": "node dist/index.js ./src",
    "lint": "eslint ./src",
    "lint:fix": "eslint --fix ./src",
    "precommit:check": "npm run lint && npm run test && npm run build && npm run archlint"
  },
  "license": "MIT",
  "dependencies": {
    "jsonschema": "^1.5.0"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^4.4.1",
    "@types/jest": "^29.5.14",
    "eslint": "^9.29.0",
    "globals": "^16.2.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.0",
    "typescript": "^4.9.5",
    "typescript-eslint": "^8.34.1"
  }
}
